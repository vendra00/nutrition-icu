services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=nutrition_icu_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=vendra'
    ports:
      - '5432:5432'
